import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    pokeNames:{
        textAlign: 'center',
        marginTop: 10,
        textTransform: 'capitalize',
        fontSize: 18,
        fontWeight: 'bold'
    },

    container:{
        marginTop: 50,
    }
});